{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs');const p='../api-zod/src/index.ts';let c=fs.readFileSync(p,'utf8');fs.writeFileSync(p,c.replace(/export \\* from '\\.\\/generated\\/types';/g,''));\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.30.0"
  }
}
